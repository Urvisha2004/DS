python -m venv .venv
pip install py-solc-x
pip install web3


1. Using Blockchain.com Explorer
Step 1: Open Blockchain.com

Open any browser

Go to: https://www.blockchain.com/

Blockchain.com is a blockchain explorer used to view blocks, transactions, addresses, and miners on different blockchains like Bitcoin and Ethereum.

Step 2: Go to Explorer Menu

On the homepage, click on “Explorer”

Select the blockchain (Bitcoin or Ethereum)

2.1 View Blocks

Click on Blocks

A list of recent blocks is displayed

Each block contains:

Block Height

Timestamp

Number of transactions

Block size

Miner information

2.1.1 Show Miner

When a block is opened, the Miner / Validator name is shown

Miner is responsible for:

Validating transactions

Creating new blocks

Securing the blockchain network

2.2 View Transactions

Click on Transactions

A list of recent transactions is shown

2.2.1 Show Transaction Details

When you open a transaction, it displays:

Transaction Hash

From Address

To Address

Amount transferred

Transaction Fee

Block confirmation status

Timestamp

This helps to verify and track blockchain transactions.

Step 3: Search Specific Block and Transaction [Unit–1]

Use the Search bar at the top

Paste:

Block hash → view block details

Transaction hash → view transaction details

Wallet address → view address balance and history

This feature provides full transparency, a key property of blockchain.

2. ETH Converter (eth-converter.com)
Website: https://eth-converter.com/

ETH Converter is used to convert:

Ether (ETH)

Wei

Gwei

USD and other currencies

Explanation

1 ETH = 10¹⁸ Wei

Helps developers calculate transaction fees

Useful for gas fee understanding

Example:

0.01 ETH → Wei / USD value

3. MetaMask Installation and ETH Wallet
Install MetaMask

Open Chrome / Brave

Go to Chrome Web Store

Search MetaMask

Install the extension

Create Wallet

Create a new wallet

Secure Secret Recovery Phrase

Set password

Get ETH

Switch to Sepolia Test Network

Use faucet to receive test ETH

ETH is visible in MetaMask wallet

MetaMask acts as:

Crypto wallet

Web3 gateway

Blockchain authentication tool

4. Install Ganache (Windows OS)
What is Ganache?

Ganache is a local Ethereum blockchain

Used for testing smart contracts

Installation Steps

Go to: https://trufflesuite.com/ganache

Download Windows version

Install and open

Features

Provides test accounts

Each account has ETH

Displays transactions, blocks, and gas usage

Ganache helps developers test without real ETH.

5. Remix Ethereum IDE
Website: https://remix.ethereum.org/
Explanation

Remix is a browser-based Solidity IDE

Used to:

Write smart contracts

Compile contracts

Deploy and test contracts

Features

Solidity Editor

Compiler

Deployment environment

Debugger

Remix supports MetaMask and Ganache networks.

6. Etherscan
Website: https://etherscan.io/
Explanation

Etherscan is an Ethereum blockchain explorer

Used to:

Track transactions

View smart contracts

Check wallet balances

Monitor gas fees

Key Information Shown

Transaction hash

Block number

Gas used

Contract source code

Etherscan ensures network transparency.

7. Blockchain Visual Explanation Tool
Website: https://andersbrownworth.com/blockchain
Explanation

This website visually explains how blockchain works

Shows:

Blocks

Hashes

Mining process

Chain immutability

Benefits

Easy understanding of:

Block linking

Proof of Work

Tamper resistance

This site is very useful for beginners and students.

ASS3:-BASIC SOLIDITY:-
------------------
// SPDX-License-Identifier: MIT
pragma solidity >=0.8.0 <0.9.0;


contract ass3 {

    uint256 favoriteNumber;

    // This is a comment!
    struct People {
        uint256 favoriteNumber;
        string name;
    }

    People[] public people;
    mapping(string => uint256) public nameToFavoriteNumber;

    function store(uint256 _favoriteNumber) public {
        favoriteNumber = _favoriteNumber;
    }
    
    function retrieve() public view returns (uint256){
        return favoriteNumber;
    }

    function addPerson(string memory _name, uint256 _favoriteNumber) public {
        people.push(People(_favoriteNumber, _name));
        nameToFavoriteNumber[_name] = _favoriteNumber;
    }
}
______________________________________________________________
______________________________________________________________
ASS4:-STORAGE FACTORY:-
// SPDX-License-Identifier: MIT

pragma solidity >=0.8.0 <0.9.0;

import "./ass3.sol"; 

contract ass4 is ass3 {
    
    ass3[] public simpleStorageArray;
    
    function createSimpleStorageContract() public {
        ass3 a3 = new ass3();
        simpleStorageArray.push(a3);
    }
    
    function sfStore(uint256 _simpleStorageIndex, uint256 _simpleStorageNumber) public {
        // Address 
        // ABI 
        //this line has an explicit cast to the address type and initializes a new SimpleStorage object from the address
        ass3(address(simpleStorageArray[_simpleStorageIndex])).store(_simpleStorageNumber); 

        //this line simply gets the SimpleStorage object at the index _simpleStorageIndex in the array simpleStorageArray
        //simpleStorageArray[_simpleStorageIndex].store(_simpleStorageNumber);
    }
    
    function sfGet(uint256 _simpleStorageIndex) public view returns (uint256) {
        //this line has an explicit cast to the address type and initializes a new SimpleStorage object from the address 
        return ass3(address(simpleStorageArray[_simpleStorageIndex])).retrieve(); 

        //this line simply gets the SimpleStorage object at the index _simpleStorageIndex in the array simpleStorageArray
        //return simpleStorageArray[_simpleStorageIndex].retrieve(); 
    }
}
_______________________________________________________________________________________________________________________
_______________________________________________________________________________________________________________________
ASS5:-FUND ME:-

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

/*
   FUND ME CONTRACT
   ----------------
   Concepts:
   • payable → allows function to receive ETH
   • msg.sender → who is sending the transaction
   • msg.value → how much ETH (in wei) was sent
   • ETH units → wei, gwei, ether
*/

contract FundMe {
    // Keep track of who funded and how much
    mapping(address => uint256) public addressToAmountFunded;
    address[] public funders;

    // Minimum contribution (0.01 ETH)
    uint256 public constant MINIMUM_USD = 0.01 ether;

    // Fund the contract
    function fund() public payable {
        // msg.value is in wei → check minimum contribution
        require(msg.value >= MINIMUM_USD, "Send at least 0.01 ETH!");

        // msg.sender = address of the sender
        addressToAmountFunded[msg.sender] += msg.value;

        // Add new funder
        funders.push(msg.sender);
    }

    // Show balance of contract
    function getBalance() public view returns (uint256) {
        return address(this).balance; // total ETH stored
    }

    // Withdraw funds (only owner in real use)
    function withdraw() public {
        // send all ETH to the caller (msg.sender)
        payable(msg.sender).transfer(address(this).balance);
    }
}
_____________________________________________________________________________________
__________________________________________________________________________________________________
ASS6:-// SPDX-License-Identifier: MIT
//https://docs.chain.link/data-feeds/price-feeds/addresses
//ethereum price in usd today

pragma solidity ^0.8.7;

import "@chainlink/contracts/src/v0.8/interfaces/AggregatorV3Interface.sol";

contract ass6 {
    AggregatorV3Interface internal priceFeed;

    /**
     * Network: Ethereum Sepolia Testnet
     * Aggregator: ETH/USD
     * Address: 0x694AA1769357215DE4FAC081bf1f309aDC325306
     */
    constructor() {
        priceFeed = AggregatorV3Interface(
          0x694AA1769357215DE4FAC081bf1f309aDC325306
        );
    }

    /**
     * Returns the latest ETH/USD price without extra decimals
     * Example: 2435.78 instead of 243578000000
     */
    function getLatestPrice() public view returns (int256) {
        (
            ,int256 price,,, 
            
        ) = priceFeed.latestRoundData();

        // Chainlink price has 8 decimals, divide to show actual price
        return price / 1e8;
    }
}
___________________________________________________________________________________
______________________________________________________________________________________
ASS7:-IMPORT FROM NPM AND ADVANCE SOLIDITY

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

// Importing from NPM: Chainlink interface for price feeds
import "@chainlink/contracts/src/v0.8/interfaces/AggregatorV3Interface.sol";

contract AdvancedFundMe7 {
    // Mapping to track how much ETH each address has funded
    mapping(address => uint256) public addressToAmountFunded; // Advanced Solidity: mapping

    // Array to keep track of all funder addresses
    address[] public funders; // Advanced Solidity: dynamic array

    // Owner of the contract (the deployer)
    address public owner; // Advanced Solidity: state variable for contract owner

    // Constructor: executed once when contract is deployed
    constructor() {
        owner = msg.sender; // msg.sender is deployer, sets contract owner
    }

    // Fund function: anyone can send ETH
    function fund() public payable {
        require(msg.value > 0, "You need to send some ETH!"); // Require: threshold check
        addressToAmountFunded[msg.sender] += msg.value; // Update mapping with funded amount
        funders.push(msg.sender); // Add funder to array
    }

    // Get Chainlink price feed version
    function getVersion() public view returns (uint256) {
        // Interface usage: AggregatorV3Interface
        AggregatorV3Interface priceFeed = AggregatorV3Interface(
            0x694AA1769357215DE4FAC081bf1f309aDC325306 // Sepolia ETH/USD price feed address
        );
        return priceFeed.version(); // Call interface function
    }

    // Get latest ETH/USD price
    function getPrice() public view returns (uint256) {
        AggregatorV3Interface priceFeed = AggregatorV3Interface(
            0x694AA1769357215DE4FAC081bf1f309aDC325306
        );
        // Tuple destructuring: ignore unused variables with commas
        (, int256 answer, , , ) = priceFeed.latestRoundData(); // latestRoundData returns tuple
        return uint256(answer * 1e10); // Convert 8 decimals to 18 decimals (matching units)
    }

    // Convert ETH amount to USD
    function getConversionRate(
        uint256 ethAmount
    ) public view returns (uint256) {
        uint256 ethPrice = getPrice(); // Call getPrice()
        uint256 ethAmountInUsd = (ethPrice * ethAmount) / 1e18; // Match ETH units (wei -> ether)
        return ethAmountInUsd;
    }

    // Modifier to restrict functions to only owner
    modifier onlyOwner() {
        require(msg.sender == owner, "Not contract owner"); // Equality check (==)
        _; // Continue execution of the function
    }

    // Withdraw all ETH from contract (owner only)
    function withdraw() public onlyOwner {
        // Transfer ETH using call (recommended over transfer/send)
        (bool success, ) = payable(msg.sender).call{
            value: address(this).balance
        }("");
        require(success, "Transfer failed"); // Revert if transfer fails

        // Reset mapping for all funders
        for (uint256 i = 0; i < funders.length; i++) {
            // For loop, array length
            address funder = funders[i];
            addressToAmountFunded[funder] = 0; // Reset funded amount
        }

        funders = new address[](0); // Reset funders array
    }

    // Helper function: get contract balance
    function getBalance() public view returns (uint256) {
        return address(this).balance; // 'this' refers to the contract itself
    }
}

________________________________________________________________________________
________________________________________________________________________________
ASS8:-WEB3.PY INSTALLATION VSCODE,PYTHON AND WEB3
__________________________________________________________________________________
__________________________________________________________________________________
1️⃣ Install MetaMask (Browser Wallet)
2️⃣ Install Visual Studio Code (VSCode)
3️⃣ VSCode Crash Course (Basics)
Double click → VSCode opens
Right click → New Folder → web3_py
File → Open Folder → web3_py
4.install python
👉 https://www.python.org/downloads/
python --version

8️⃣ Install Node.js (for blockchain tools)	
👉 https://nodejs.org/
node -v
npm -v
9️⃣ Terminal Basics (Very Important)	
Ctrl + `
cd foldername     # enter folder
cd ..             # go back
dir               # list files (Windows)
ls                # list files (Mac/Linux)
mkdir project     # create folder
🔟 Create Project Folder
mkdir web3_py_simple_storage
cd web3_py_simple_storage
code .
1️⃣1️⃣ Create New Files
simple_storage.sol
deploy.py
1️⃣4️⃣ Solidity Compiler Version (VSCode)
1️⃣5️⃣ Solidity Formatting (Prettier)	
Enable Format on Save
"editor.formatOnSave": true
1️⃣6️⃣ Python Formatting (Black)
pip install black
VSCode Settings
"python.formatting.provider": "black"
1️⃣7️⃣ Install Web3.py
python -m pip install --upgrade pip
pip install web3
1️⃣8️⃣ Install python-dotenv (optional but recommended)
pip install python-dotenv
pip install:-
python -m pip install --upgrade pip
_____________________________________________________________
______________________________________________________________
(.venv) python -m pip install python-dotenv
(.venv) pip show python-dotenv
pip install web3 py-solc-x python-dotenv
python -m pip install python-dotenv
pip install python-dotenv

-----------------------------------------------
import json
import os
from dotenv import load_dotenv
from web3 import Web3
from web3.middleware import ExtraDataToPOAMiddleware
from solcx import compile_standard, install_solc

# ----------------------------
# Load environment variables
# ----------------------------
load_dotenv()

PRIVATE_KEY = os.getenv("PRIVATE_KEY")
MY_ADDRESS = os.getenv("MY_ADDRESS")
RPC_URL = os.getenv("RPC_URL")
CHAIN_ID = int(os.getenv("CHAIN_ID"))

if not all([PRIVATE_KEY, MY_ADDRESS, RPC_URL, CHAIN_ID]):
    raise Exception("Please set PRIVATE_KEY, MY_ADDRESS, RPC_URL, CHAIN_ID in .env file")

# ----------------------------
# Read Solidity contract
# ----------------------------
with open("SimpleStorage.sol", "r") as file:
    simple_storage_file = file.read()

# ----------------------------
# Install Solidity compiler
# ----------------------------
print("Installing Solidity compiler 0.6.0...")
install_solc("0.6.0")

# ----------------------------
# Compile Solidity contract
# ----------------------------
compiled_sol = compile_standard(
    {
        "language": "Solidity",
        "sources": {"SimpleStorage.sol": {"content": simple_storage_file}},
        "settings": {
            "outputSelection": {
                "*": {"*": ["abi", "metadata", "evm.bytecode", "evm.bytecode.sourceMap"]}
            }
        },
    },
    solc_version="0.6.0",
)

# Save compiled contract for reference
with open("compiled_code.json", "w") as file:
    json.dump(compiled_sol, file, indent=4)

# ----------------------------
# Extract ABI and Bytecode
# ----------------------------
bytecode = compiled_sol["contracts"]["SimpleStorage.sol"]["SimpleStorage"]["evm"]["bytecode"]["object"]
abi = json.loads(
    compiled_sol["contracts"]["SimpleStorage.sol"]["SimpleStorage"]["metadata"]
)["output"]["abi"]

# ----------------------------
# Connect to blockchain
# ----------------------------
w3 = Web3(Web3.HTTPProvider(RPC_URL))

# Inject PoA middleware only for PoA networks (Goerli / Sepolia)
if CHAIN_ID in [5, 11155111]:  # 5 = Goerli, 11155111 = Sepolia
    w3.middleware_onion.inject(ExtraDataToPOAMiddleware, layer=0)

if not w3.is_connected():
    raise Exception("Failed to connect to blockchain")

print("Connected to blockchain. Client:", w3.client_version)

# ----------------------------
# Deploy the contract
# ----------------------------
SimpleStorage = w3.eth.contract(abi=abi, bytecode=bytecode)
nonce = w3.eth.get_transaction_count(MY_ADDRESS)

# Build deployment transaction
transaction = SimpleStorage.constructor().build_transaction({
    "chainId": CHAIN_ID,
    "from": MY_ADDRESS,
    "nonce": nonce,
    "gasPrice": w3.eth.gas_price,
})

# Sign transaction
signed_txn = w3.eth.account.sign_transaction(transaction, PRIVATE_KEY)
tx_hash = w3.eth.send_raw_transaction(signed_txn.raw_transaction)

print("Deploying contract...")
tx_receipt = w3.eth.wait_for_transaction_receipt(tx_hash)
print("Contract deployed at:", tx_receipt.contractAddress)

# ----------------------------
# Interact with deployed contract
# ----------------------------
simple_storage = w3.eth.contract(
    address=tx_receipt.contractAddress,
    abi=abi
)

# Read initial value
initial_value = simple_storage.functions.retrieve().call()
print("Initial Stored Value:", initial_value)

# Store new value
store_txn = simple_storage.functions.store(15).build_transaction({
    "chainId": CHAIN_ID,
    "from": MY_ADDRESS,
    "nonce": nonce + 1,
    "gasPrice": w3.eth.gas_price,
})

signed_store_txn = w3.eth.account.sign_transaction(store_txn, PRIVATE_KEY)
tx_store_hash = w3.eth.send_raw_transaction(signed_store_txn.raw_transaction)  # <-- fixed
w3.eth.wait_for_transaction_receipt(tx_store_hash)

updated_value = simple_storage.functions.retrieve().call()
print("Updated Stored Value:", updated_value)    






